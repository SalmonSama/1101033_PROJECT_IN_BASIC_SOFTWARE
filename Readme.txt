แน่นอนครับ จากข้อมูลที่คุณให้มา ผมได้นำมาจัดรูปแบบและเพิ่มเติมรายละเอียดเพื่อให้เป็นเอกสาร README ที่สมบูรณ์และเป็นมืออาชีพมากขึ้น เหมาะสำหรับใช้ใน GitHub หรือเป็นคู่มือการติดตั้งโปรเจคครับ

-----

# เว็บไซต์ร้านข้าวขาหมูลุงรี

โปรเจคสำหรับพัฒนาและทดสอบเว็บไซต์ร้านข้าวขาหมูลุงรีบนเครื่องคอมพิวเตอร์ส่วนตัว (Local Development)

## \#️⃣ ข้อมูลทางเทคนิค (Technical Specifications)

  * **WordPress Version:** `6.4.3`
  * **XAMPP Version:** `8.2.12`

## 🚀 การติดตั้ง (Installation Guide)

ทำตามขั้นตอนต่อไปนี้เพื่อติดตั้งและรันโปรเจคบนเครื่องของคุณ

### 1\. ติดตั้งโปรแกรมพื้นฐาน

  * ดาวน์โหลดและติดตั้ง **XAMPP** เวอร์ชัน 8.2.12 หรือสูงกว่า จาก [เว็บไซต์อย่างเป็นทางการ](https://www.apachefriends.org/index.html)
  * หลังจากติดตั้งเสร็จสิ้น ให้เปิด XAMPP Control Panel และกด **Start** ที่โมดูล **Apache** และ **MySQL**

### 2\. คัดลอกไฟล์โปรเจค

  * คัดลอกโฟลเดอร์ `khamulungree` ทั้งหมดไปวางไว้ที่ไดเรกทอรี `htdocs` ของ XAMPP
      * *ตัวอย่างตำแหน่ง:* `C:\xampp\htdocs\`

### 3\. สร้างและตั้งค่าฐานข้อมูล

  * เปิดเว็บเบราว์เซอร์แล้วไปที่ `http://localhost/phpmyadmin/`
  * คลิกที่ **New** เพื่อสร้างฐานข้อมูลใหม่
  * ในช่อง **Database name** ให้ตั้งชื่อเป็น `sql_khamulungree`
  * ในช่อง **Collation** ให้เลือกเป็น `utf8_general_ci`
  * คลิกปุ่ม **Create**

### 4\. นำเข้าข้อมูล (Import Database)

  * หลังจากสร้างฐานข้อมูลเสร็จแล้ว ให้คลิกเลือกฐานข้อมูล `sql_khamulungree` จากเมนูด้านซ้าย
  * ไปที่แท็บ **Import** ด้านบน
  * คลิกที่ปุ่ม **Choose File** และเลือกไฟล์ฐานข้อมูล `.sql` ของโปรเจค
  * เลื่อนลงมาด้านล่างสุดและคลิกปุ่ม **Import**

### 5\. ตรวจสอบการตั้งค่า WordPress

  * เปิดไฟล์ `wp-config.php` ที่อยู่ในโฟลเดอร์ `khamulungree`
  * ตรวจสอบว่าการตั้งค่าการเชื่อมต่อฐานข้อมูลถูกต้องตรงกับค่าเริ่มต้นของ XAMPP ดังนี้:
    ```php
    /** The name of the database for WordPress */
    define( 'DB_NAME', 'sql_khamulungree' );

    /** Database username */
    define( 'DB_USER', 'root' );

    /** Database password */
    define( 'DB_PASSWORD', '' );

    /** Database hostname */
    define( 'DB_HOST', 'localhost' );
    ```

## 🏃‍♂️ การเข้าใช้งานเว็บไซต์

หลังจากทำตามขั้นตอนทั้งหมดเรียบร้อยแล้ว คุณสามารถเข้าถึงเว็บไซต์ได้ผ่านทาง:

**`http://localhost/khamulungree/`**
